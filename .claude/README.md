# .claude/

This directory contains configuration specific to **Claude Code** (claude.ai/code).

- `settings.local.json`: Tool permissions and allowed actions for Claude when working in this repository.

For general project instructions that work across multiple AI agents (Claude, Cursor, Copilot, etc.), see the root `AGENTS.md` file instead.
