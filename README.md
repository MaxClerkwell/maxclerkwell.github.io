# maxclerkwell.github.io

Personal site and blog of Stephan Bökelmann, built with Jekyll and hosted on GitHub Pages.

## Local development

Run the local development server with:

```bash
./scripts/serve.sh
```

The script checks for Ruby, Bundler, and all gem dependencies, installs anything missing, and then starts Jekyll with live reload at `http://localhost:4000`.
