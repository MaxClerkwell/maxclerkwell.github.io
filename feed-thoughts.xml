---
layout: none
permalink: /feed-thoughts.xml
sitemap: false
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title>{{ site.title | xml_escape }} — Thoughts</title>
  <subtitle>Essays on ideas rather than hardware: what physics can and cannot explain, autonomous will, consciousness, and how to work.</subtitle>
  <link href="{{ '/feed-thoughts.xml' | absolute_url }}" rel="self" type="application/atom+xml"/>
  <link href="{{ '/thoughts/' | absolute_url }}" rel="alternate" type="text/html"/>
  <id>{{ '/thoughts/' | absolute_url }}</id>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <author>
    <name>{{ site.author.name | default: site.author | xml_escape }}</name>
    <email>{{ site.author.email | xml_escape }}</email>
    <uri>{{ site.author.uri | xml_escape }}</uri>
  </author>
  <generator uri="https://jekyllrb.com/">Jekyll</generator>

  {% include get-sections.html %}
  {% assign feed_limit = site.feed.posts_limit | default: 20 %}

  {% for post in thoughts_posts limit: feed_limit %}
  <entry>
    <title>{{ post.title | xml_escape }}</title>
    <link href="{{ post.url | absolute_url }}" rel="alternate" type="text/html"/>
    <id>{{ post.url | absolute_url }}</id>
    <published>{{ post.date | date_to_xmlschema }}</published>
    <updated>{{ post.date | date_to_xmlschema }}</updated>
    {% if post.description %}
    <summary>{{ post.description | xml_escape }}</summary>
    {% endif %}
    {% for tag in post.tags %}
    <category term="{{ tag | xml_escape }}"/>
    {% endfor %}
    <content type="text">{{ post.content | strip_html | normalize_whitespace | truncate: 800 | xml_escape }}</content>
  </entry>
  {% endfor %}

</feed>
