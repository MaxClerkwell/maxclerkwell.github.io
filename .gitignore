_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata
vendor/
Gemfile.lock
.bundle/

# AI / Agent local configuration (Claude Code, Cursor, etc.)
# Keep AGENTS.md and CLAUDE.md tracked — they are project documentation.
# Only ignore truly local/per-user agent settings:
.claude/settings.local.json
.cursor/rules/local*
